<h1>Songs</h1>

<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= song_path(song) %>">
        <%= song.artist.name %>
        -
        <%= song.title %>
      </a>
      <%= link_to "#{song.artist.name}-#{song.title}", song_path(song) %>
    </li>
  <% end %>
</ul>